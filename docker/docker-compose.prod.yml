version: '3.8'

services:
  # CTA Anomaly Detection API
  api:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    container_name: cta-anomaly-api-prod
    expose:
      - "8000"
    volumes:
      - cta_data:/app/data:ro
      - cta_models:/app/models
    environment:
      - PYTHONPATH=/app
      - ENVIRONMENT=production
    command: ["uvicorn", "src.api.app:app", "--host", "0.0.0.0", "--port", "8000", "--workers", "4"]
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    restart: always
    deploy:
      resources:
        limits:
          memory: 1G
          cpus: '0.5'

  # Streamlit Dashboard
  dashboard:
    build:
      context: ..
      dockerfile: docker/Dockerfile.dashboard
    container_name: cta-anomaly-dashboard-prod
    expose:
      - "8501"
    volumes:
      - cta_data:/app/data:ro
      - cta_models:/app/models:ro
    environment:
      - PYTHONPATH=/app
      - API_BASE_URL=http://api:8000
      - ENVIRONMENT=production
    command: ["streamlit", "run", "src/dashboard/dashboard.py", "--server.address", "0.0.0.0", "--server.port", "8501"]
    depends_on:
      api:
        condition: service_healthy
    restart: always
    deploy:
      resources:
        limits:
          memory: 512M
          cpus: '0.25'

  # Data Collection Service (scheduled via cron)
  data-collector:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    container_name: cta-data-collector-prod
    volumes:
      - cta_data:/app/data
    environment:
      - PYTHONPATH=/app
      - ENVIRONMENT=production
    command: ["python", "src/data_collection/fetch_data.py"]
    restart: "no"
    profiles:
      - data-collection
    deploy:
      resources:
        limits:
          memory: 256M
          cpus: '0.1'

  # Nginx reverse proxy
  nginx:
    image: nginx:alpine
    container_name: cta-nginx-prod
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      - api
      - dashboard
    restart: always

volumes:
  cta_data:
    driver: local
  cta_models:
    driver: local

networks:
  default:
    name: cta-anomaly-network-prod