# PyTorch Lightning LSTM Dependencies - Apple Silicon (MPS) Optimized
# For macOS with Apple Silicon (M1/M2/M3)

# PyTorch with MPS support (requires macOS 12.3+)
torch>=2.0.0
torchvision>=0.15.0
torchaudio>=2.0.0

# PyTorch Lightning and metrics
pytorch-lightning>=2.0.0
torchmetrics>=0.11.0

# Additional utilities
tensorboard>=2.10.0
lightning[extra]>=2.0.0

# MPS-specific optimizations
# Note: Install with: pip install -r requirements-lstm-mps.txt